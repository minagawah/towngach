const { SEXAGENARY_CYCLE } = require('../index');

describe('A test suite for: sexagen/sexagen', () => {
  it('exports SEXAGENARY_CYCLE', () => {
    expect(SEXAGENARY_CYCLE).toBeDefined();
  });
});

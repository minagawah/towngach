const { ELEMENTS } = require('../src/elem/elem.js');
const { LOCALES, Localizer } = require('../src/locale/Localizer');
console.log(ELEMENTS);

// const l = Localizer();
// for (const elementKey of Object.keys(ELEMENTS)) {
  // loc.set_english('hello', 'bye');
  // console.log(loc.get_english_value());
